import React from 'react'

import { Layout } from '../../components/Layout'

const Help = () => (
  <Layout>
    <div
      style={{
        textAlign: 'center',
        minHeight: '10vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <h1>Help</h1>
    </div>
  </Layout>
)

export default Help
