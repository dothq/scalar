import React from 'react'

import assets from '../../assets/home'

const steps = {
  windows: [
    {
      title: 'Open installer',
      description: {
        chrome: 'Click the ',
      },
    },
  ],
}

export const InstallStepGrid = ({ os }: { os?: string }) => {
  return <div></div>
}
